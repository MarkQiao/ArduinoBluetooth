<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="?selectableItemBackground"
    android:paddingBottom="4.0dip"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="4.0dip">

    <ImageView
        android:tint="@color/chocolate"
        android:id="@+id/deviceIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8.0dip"
        android:src="@drawable/ic_devices_other_black_24dp" />

    <TextView
        android:id="@+id/availableDeviceTitle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginTop="6.0dip"
        android:layout_toRightOf="@id/deviceIcon"
        android:ellipsize="end"
        android:lines="1"
        android:textColor="#ff000000"
        android:textSize="18.0dip" />

    <TextView
        android:id="@+id/availableDeviceSubtitle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/availableDeviceTitle"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginTop="4.0dip"
        android:layout_toRightOf="@id/deviceIcon"
        android:ellipsize="end"
        android:lines="1"
        android:textSize="16.0dip" />
</RelativeLayout>